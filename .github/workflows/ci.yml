name: NextGRCv2 CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build-laravel:
    name: 🐘 Build Laravel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Copy .env
        run: cp .env.example .env

      - name: Generate app key
        run: php artisan key:generate

  build-docker:
    name: 🐳 Build Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Compose
        run: docker compose build

  build-flask:
    name: 🐍 Build Flask ML
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r ml/requirements.txt

  deploy:
    name: 🚀 Deploy to VM
    runs-on: self-hosted
    needs: [build-laravel, build-docker, build-flask]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          cd $GITHUB_WORKSPACE
          docker compose down --remove-orphans
          docker compose build
          nohup docker compose up -d > /tmp/deploy.log 2>&1
          sleep 20
          docker compose ps
          echo "✅ Deploy terminé !"